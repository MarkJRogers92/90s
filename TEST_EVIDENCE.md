# Test evidence

## Historical M0–M2 evidence

The prior TEST_EVIDENCE.md is preserved byte-for-byte at [docs/testing/M0_M2_HISTORICAL_EVIDENCE.md](docs/testing/M0_M2_HISTORICAL_EVIDENCE.md). Its last M2 gate reported 123 Vitest tests, 12 Chromium browser checks, typecheck, build, production scan, and preview inspection. Those are historical results, not tests rerun for this checkpoint.

## 2026-09-13 — M3A transaction kernel (isolated workspace)

Environment: Node v22.16.0, TypeScript 5.8.3. The project's pinned TypeScript is 7.0.2; the installed pinned dependency tree was not available in this environment. This checkpoint uses no new dependency.

RED evidence:

- A no-behavior scaffold compiled successfully; the real purchase contract failed with ERR_ASSERTION `false !== true`, exit 1. It was not a missing import, test-runner failure, or mocked assertion.
- Applying the repository's additional indexed-access flag exposed TS18048 for a possibly missing indexed offer. An explicit missing-offer guard resolved the root cause.

GREEN evidence:

- `node tools/test-m3-transactions.mjs /opt/nvm/versions/node/v22.16.0/bin/tsc` — exit 0.
- Strict standalone compilation with `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, and `isolatedModules` — exit 0.
- Node contract runner — 24 tests, 24 passed, zero failed/cancelled/skipped.
- Contracts cover purchase debit/exact funds/zero price/insufficient funds; theft/capped Heat/zero Heat; Leave; repeat and cross-action acquisition prevention; unknown/malformed actions; input and result immutability; duplicate IDs; distinct copies of one definition; monetary/Heat/identity validation; empty stock; deterministic replay.
- The runner emits to a temporary CommonJS directory and cleans it on completion. No game source, dependency, runtime connection, or build artifact is generated by running the test.

Not run: pinned full-project typecheck, existing Vitest suite, Playwright/browser tests, production build/preview, Safari/device coverage, or user feel checks. Network/DNS prevented a local GitHub clone and dependency setup. The M3 module is not connected to live gameplay; no playable-M3 claim is made.

## Approved art archive

The supplied ZIP contains ten original 1536 x 1024 PNG boards and nine earlier supporting images. Every image was opened/verified and every archived image byte stream matched its SHA-256 manifest entry. A reference contact sheet was visually reviewed for catalog accuracy. ZIP integrity passed.

Archive: `DEAD_MALL_APPROVED_ART_REFERENCE_PACK.zip` (24,016,074 bytes).

SHA-256: `af6359b11fcbc6faa11878594ed16a28acfe91a365f38f331163e26f310b059f`.

These checks prove the supplied ZIP, not a GitHub binary upload. The images themselves remain pending import. The manifest documents the one earlier generated result not separately recoverable because its filename was reused. No asset has been tested as a transparent, frame-aligned Aseprite runtime sprite.
